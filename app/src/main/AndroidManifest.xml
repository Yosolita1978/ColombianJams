<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.yosola.colombianjams">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AllSongsActivity"
            android:label="@string/category_allsongs"/>

        <activity android:name=".GenreActivity"
            android:label="@string/category_genres"/>

        <activity android:name=".ArtistsActivity"
            android:label="@string/category_artist"/>

        <activity android:name=".PlaySingleSongActivity"
            android:label="@string/playsong"/>


    </application>


</manifest>